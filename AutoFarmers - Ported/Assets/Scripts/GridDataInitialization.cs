using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Unity.Entities;

public class GridDataInitialization : MonoBehaviour
{
    //public GameObject gridDataPrefab;
    Entity convertedGridData;

    // Start is called before the first frame update
    void Start()
    {
        //convertedGridData = World.Active.EntityManager.CreateEntity();
        //convertedGridData = GameObjectConversionUtility.ConvertGameObjectHierarchy(gridDataPrefab, World.Active);
        //World.Active.EntityManager.AddComponentData(convertedGridData,
        //    new GridData(512, 512));
       
    }

    
}
